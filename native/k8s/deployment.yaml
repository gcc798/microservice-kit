apiVersion: apps/v1
kind: Deployment
metadata:
  name: microservice-kit-gateway
spec:
  replicas: 2
  selector:
    matchLabels: { app: microservice-kit-gateway }
  template:
    metadata:
      labels: { app: microservice-kit-gateway }
    spec:
      terminationGracePeriodSeconds: 40
      containers:
        - name: gateway
          image: ghcr.io/your-org/microservice-kit-gateway:0.1.0
          ports:
            - { name: http, containerPort: 9009 }
          env:
            - { name: MS_K_APP_ENV, value: prod }
            - { name: MS_K_SERVER_PORT, value: "9009" }
            - { name: MS_K_REGISTRY_DRIVER, value: consul }
            - name: MS_K_REGISTRY_ADDRESS
              valueFrom:
                configMapKeyRef: { name: microservice-kit-config, key: registry-address }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits: { cpu: 500m, memory: 512Mi }
          startupProbe:
            httpGet: { path: /health/startup, port: http }
            periodSeconds: 2
            failureThreshold: 15
          livenessProbe:
            httpGet: { path: /health/live, port: http }
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /health/ready, port: http }
            periodSeconds: 5
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: microservice-kit-iam
spec:
  replicas: 1
  selector:
    matchLabels: { app: microservice-kit-iam }
  template:
    metadata:
      labels: { app: microservice-kit-iam }
    spec:
      terminationGracePeriodSeconds: 40
      containers:
        - name: iam
          image: ghcr.io/your-org/microservice-kit-iam:0.1.0
          ports:
            - { name: http, containerPort: 9010 }
            - { name: grpc, containerPort: 9110 }
          env:
            - { name: MS_K_APP_ENV, value: prod }
            - { name: MS_K_SERVER_PORT, value: "9010" }
            - { name: MS_K_GRPC_PORT, value: "9110" }
            - name: MS_K_SERVICE_ADVERTISE_HOST
              valueFrom:
                fieldRef: { fieldPath: status.podIP }
            - { name: MS_K_REGISTRY_DRIVER, value: consul }
            - name: MS_K_REGISTRY_ADDRESS
              valueFrom:
                configMapKeyRef: { name: microservice-kit-config, key: registry-address }
            - name: MS_K_DATABASE_DSN
              valueFrom:
                secretKeyRef: { name: microservice-kit-secret, key: database-dsn }
            - name: MS_K_REDIS_ADDR
              valueFrom:
                configMapKeyRef: { name: microservice-kit-config, key: redis-addr }
            - name: MS_K_REDIS_PASSWORD
              valueFrom:
                secretKeyRef: { name: microservice-kit-secret, key: redis-password }
            - name: MS_K_JWT_SECRET
              valueFrom:
                secretKeyRef: { name: microservice-kit-secret, key: jwt-secret }
          resources:
            requests: { cpu: 250m, memory: 256Mi }
            limits: { cpu: "1", memory: 1Gi }
          startupProbe: &service-startup
            httpGet: { path: /health/startup, port: http }
            periodSeconds: 5
            failureThreshold: 30
          livenessProbe: &service-live
            httpGet: { path: /health/live, port: http }
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe: &service-ready
            httpGet: { path: /health/ready, port: http }
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: microservice-kit-sys
spec:
  replicas: 1
  selector:
    matchLabels: { app: microservice-kit-sys }
  template:
    metadata:
      labels: { app: microservice-kit-sys }
    spec:
      terminationGracePeriodSeconds: 40
      containers:
        - name: sys
          image: ghcr.io/your-org/microservice-kit-sys:0.1.0
          ports:
            - { name: http, containerPort: 9011 }
            - { name: grpc, containerPort: 9111 }
          env:
            - { name: MS_K_APP_ENV, value: prod }
            - { name: MS_K_SERVER_PORT, value: "9011" }
            - { name: MS_K_GRPC_PORT, value: "9111" }
            - name: MS_K_SERVICE_ADVERTISE_HOST
              valueFrom:
                fieldRef: { fieldPath: status.podIP }
            - { name: MS_K_REGISTRY_DRIVER, value: consul }
            - name: MS_K_REGISTRY_ADDRESS
              valueFrom:
                configMapKeyRef: { name: microservice-kit-config, key: registry-address }
            - name: MS_K_DATABASE_DSN
              valueFrom:
                secretKeyRef: { name: microservice-kit-secret, key: database-dsn }
            - name: MS_K_REDIS_ADDR
              valueFrom:
                configMapKeyRef: { name: microservice-kit-config, key: redis-addr }
            - name: MS_K_REDIS_PASSWORD
              valueFrom:
                secretKeyRef: { name: microservice-kit-secret, key: redis-password }
          resources:
            requests: { cpu: 200m, memory: 256Mi }
            limits: { cpu: "1", memory: 1Gi }
          startupProbe:
            httpGet: { path: /health/startup, port: http }
            periodSeconds: 5
            failureThreshold: 30
          livenessProbe:
            httpGet: { path: /health/live, port: http }
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet: { path: /health/ready, port: http }
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: microservice-kit-resource
spec:
  replicas: 1
  selector:
    matchLabels: { app: microservice-kit-resource }
  template:
    metadata:
      labels: { app: microservice-kit-resource }
    spec:
      terminationGracePeriodSeconds: 40
      containers:
        - name: resource
          image: ghcr.io/your-org/microservice-kit-resource:0.1.0
          ports:
            - { name: http, containerPort: 9012 }
            - { name: grpc, containerPort: 9112 }
          env:
            - { name: MS_K_APP_ENV, value: prod }
            - { name: MS_K_SERVER_PORT, value: "9012" }
            - { name: MS_K_GRPC_PORT, value: "9112" }
            - name: MS_K_SERVICE_ADVERTISE_HOST
              valueFrom:
                fieldRef: { fieldPath: status.podIP }
            - { name: MS_K_REGISTRY_DRIVER, value: consul }
            - name: MS_K_REGISTRY_ADDRESS
              valueFrom:
                configMapKeyRef: { name: microservice-kit-config, key: registry-address }
            - name: MS_K_DATABASE_DSN
              valueFrom:
                secretKeyRef: { name: microservice-kit-secret, key: database-dsn }
            - name: MS_K_REDIS_ADDR
              valueFrom:
                configMapKeyRef: { name: microservice-kit-config, key: redis-addr }
            - name: MS_K_REDIS_PASSWORD
              valueFrom:
                secretKeyRef: { name: microservice-kit-secret, key: redis-password }
            - name: MS_K_STORAGE_ENDPOINT
              valueFrom:
                configMapKeyRef: { name: microservice-kit-config, key: storage-endpoint }
            - name: MS_K_STORAGE_ACCESS_KEY
              valueFrom:
                secretKeyRef: { name: microservice-kit-secret, key: storage-access-key }
            - name: MS_K_STORAGE_SECRET_KEY
              valueFrom:
                secretKeyRef: { name: microservice-kit-secret, key: storage-secret-key }
          resources:
            requests: { cpu: 200m, memory: 256Mi }
            limits: { cpu: "1", memory: 1Gi }
          startupProbe:
            httpGet: { path: /health/startup, port: http }
            periodSeconds: 5
            failureThreshold: 30
          livenessProbe:
            httpGet: { path: /health/live, port: http }
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet: { path: /health/ready, port: http }
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: microservice-kit-realtime
spec:
  replicas: 2
  selector:
    matchLabels: { app: microservice-kit-realtime }
  template:
    metadata:
      labels: { app: microservice-kit-realtime }
    spec:
      terminationGracePeriodSeconds: 40
      containers:
        - name: realtime
          image: ghcr.io/your-org/microservice-kit-realtime:0.1.0
          ports:
            - { name: http, containerPort: 9013 }
            - { name: grpc, containerPort: 9113 }
          env:
            - { name: MS_K_APP_ENV, value: prod }
            - { name: MS_K_SERVER_PORT, value: "9013" }
            - { name: MS_K_GRPC_PORT, value: "9113" }
            - name: MS_K_SERVICE_ADVERTISE_HOST
              valueFrom:
                fieldRef: { fieldPath: status.podIP }
            - { name: MS_K_REGISTRY_DRIVER, value: consul }
            - name: MS_K_REGISTRY_ADDRESS
              valueFrom:
                configMapKeyRef: { name: microservice-kit-config, key: registry-address }
            - name: MS_K_REDIS_ADDR
              valueFrom:
                configMapKeyRef: { name: microservice-kit-config, key: redis-addr }
            - name: MS_K_REDIS_PASSWORD
              valueFrom:
                secretKeyRef: { name: microservice-kit-secret, key: redis-password }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits: { cpu: "1", memory: 512Mi }
          startupProbe:
            httpGet: { path: /health/startup, port: http }
            periodSeconds: 5
            failureThreshold: 30
          livenessProbe:
            httpGet: { path: /health/live, port: http }
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet: { path: /health/ready, port: http }
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: microservice-kit-consul
spec:
  serviceName: microservice-kit-consul
  replicas: 1
  selector:
    matchLabels: { app: microservice-kit-consul }
  template:
    metadata:
      labels: { app: microservice-kit-consul }
    spec:
      containers:
        - name: consul
          image: hashicorp/consul:1.21
          args: ["agent", "-server", "-bootstrap-expect=1", "-client=0.0.0.0", "-data-dir=/consul/data"]
          ports:
            - { name: http, containerPort: 8500 }
            - { name: server, containerPort: 8300 }
            - { name: serf-lan, containerPort: 8301 }
          readinessProbe:
            exec:
              command: ["consul", "members"]
            periodSeconds: 5
          volumeMounts:
            - { name: data, mountPath: /consul/data }
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests: { storage: 1Gi }
